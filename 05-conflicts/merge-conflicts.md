# Merge Conflicts

Merge conflicts happen when Git cannot automatically merge changes.

Example:
- Same file
- Same line
- Different content

Git will show conflict markers and the developer must resolve it manually.