```md
# Cloning a Repository

The `git clone` command copies a remote repository to your local machine.

```bash
git clone <repository-link>
```

 # Cloning with a custom folder name

 ```bash
 git clone <repository-link> new-project-name
```

